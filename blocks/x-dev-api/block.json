{
  "schemaVersion": "1.0",
  "id": "x-dev-api",
  "version": "1.0.0",
  "type": "backend",
  "runCommand": "node -e \"console.log('API running on port ' + process.env.PORT); setInterval(() => {}, 1000);\"",
  "env": {
    "PORT": { "required": true }
  },
  "expose": {
    "port": { "default": 4000 }
  }
}+