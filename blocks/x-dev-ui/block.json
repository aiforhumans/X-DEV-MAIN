{
  "schemaVersion": "1.0",
  "id": "x-dev-ui",
  "version": "1.0.0",
  "type": "frontend",
  "runCommand": "node -e \"console.log('UI running on port ' + process.env.PORT + ' connecting to ' + process.env.API_URL); setInterval(() => {}, 1000);\"",
  "env": {
    "API_URL": { "description": "The backend endpoint", "required": true }
  },
  "expose": {
    "port": { "default": 3000 }
  }
}